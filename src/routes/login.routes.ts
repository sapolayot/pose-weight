import { Router } from "express";

const router = Router();

// router.get("/", getAll);
// router.post("/", create);
// router.put("/:id", update);
// router.delete("/:id", remove);

export default router;
